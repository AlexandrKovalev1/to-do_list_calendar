import { formatDate } from './formatDate';

// export const getTypeOfDay = async (date: Date) => {
// 	const day = formatDate(date, 'YYYYMMDD');
//
// 	const res = await fetch(`https://isdayoff.ru/${day}?pre=[1]`);
// 	const jsonRes = await res.json();
// 	return jsonRes;
// };
